Start both gateway and app in dev mode. Equivalent to running `/start-gateway` and then `/start-app`.

Execute the following two slash commands in order:
1. First, run `/start-gateway` and wait until the gateway health endpoint is responding
2. Then, run `/start-app` and wait until the app is ready

Report the final status of both services, including any background task IDs or ports that matter.
