Start both gateway and app in dev mode. Equivalent to running `/start-gateway` then `/start-app`.

Execute the following two skills in order:
1. First, run the `/start-gateway` skill and wait for gateway to be healthy
2. Then, run the `/start-app` skill and wait for app to be ready

Report the final status of both services.
